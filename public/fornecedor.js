
let state={};
async function api(url, opts={}){const r=await fetch(url,{headers:{'Content-Type':'application/json'},...opts});const j=await r.json().catch(()=>({}));if(!r.ok) throw new Error(j.error||'Erro');return j}
async function load(){state=await api('/api/state');who.textContent=state.supplier?state.supplier.name:'';render()}
function render(){quotes.innerHTML=(state.quotes||[]).map(q=>{const sq=q.suppliers[0];return `<div class="card"><h3>${q.title}</h3><p class="muted">Status: ${sq.status}</p><table><thead><tr><th>Produto</th><th>Qtd</th><th>Marca</th><th>Valor Unit.</th><th>Prazo</th><th>Pagamento</th><th>Observação</th></tr></thead><tbody>${q.items.map(item=>{const a=(sq.answers||[]).find(x=>x.productId===item.productId)||{};return `<tr><td>${item.name}</td><td>${item.quantity} ${item.unit}</td><td><input id="brand_${q.id}_${item.productId}" value="${a.brand||''}"></td><td><input type="number" step="0.01" id="price_${q.id}_${item.productId}" value="${a.unitPrice||''}"></td><td><input id="delivery_${q.id}_${item.productId}" value="${a.deliveryTime||''}"></td><td><input id="pay_${q.id}_${item.productId}" value="${a.paymentCondition||''}"></td><td><input id="note_${q.id}_${item.productId}" value="${a.note||''}"></td></tr>`}).join('')}</tbody></table><button onclick="sendQuote('${q.id}')">Salvar cotação</button></div>`}).join('')||'<p class="muted">Nenhuma cotação disponível.</p>'}
async function sendQuote(qid){const q=state.quotes.find(x=>x.id===qid);const answers=q.items.map(item=>({productId:item.productId,brand:document.getElementById(`brand_${qid}_${item.productId}`).value,unitPrice:document.getElementById(`price_${qid}_${item.productId}`).value,deliveryTime:document.getElementById(`delivery_${qid}_${item.productId}`).value,paymentCondition:document.getElementById(`pay_${qid}_${item.productId}`).value,note:document.getElementById(`note_${qid}_${item.productId}`).value}));await api(`/api/quotes/${qid}/respond`,{method:'POST',body:JSON.stringify({answers})});alert('Cotação salva com sucesso!');await load()}
async function logout(){await fetch('/api/logout',{method:'POST'});location.href='/'}
load().catch(e=>{alert(e.message);location.href='/'});
